name: my-build
repo_url: git@github.com:osela/app-repo.git
steps:
- name: build
  image: "golang:1.11"
  env:
  - name: GOPATH
    value: "/workspace/go"
  script: |
    make build
artifacts:
- name: my-artifact
  type: docker
  dockerfile: "Dockerfile"